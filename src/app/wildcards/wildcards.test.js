'use strict';
/* jshint expr:true */

describe('Service: Wildcards', function() {

  // load the service's module
  beforeEach(module('voyager2'));

  // instantiate service
  var Wildcards;
  beforeEach(inject(function(_Wildcards_) {
    Wildcards = _Wildcards_;
  }));
});
